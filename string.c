#include<stdio.h>
int main(){
    // char ch[10];
    // int i;
    // for(i=0; i<10; i++){
     // scanf("%c",&ch[i]);
    // }
    // for(i=0; i<10; i++){
     //    printf("%c\n",ch[i]);
     
     char ch[10];
     scanf("%s",&ch);
     printf("%s",ch);

   // char ch[100];
   // gets(ch);
    // puts(ch);

    return 0;
}
